# use this to run only the two following scripts - for when ned structures are added to an already
# existing set-up dir, where parameters dir (and all files within) already have been created - this is amongst things 
# done by master.sh

python generate_structures.py

python solvate.py
